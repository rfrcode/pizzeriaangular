import { RuleDirective } from './rule.directive';

describe('RuleDirective', () => {
  it('should create an instance', () => {
    const directive = new RuleDirective();
    expect(directive).toBeTruthy();
  });
});
